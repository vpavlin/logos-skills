---
name: logos-publish-artifacts
description: "Use to update self-hosted distribution repos with an app's latest BUILT artifacts — publish Basecamp module .lgx(s) to a LAN/self-hosted Basecamp package repo and/or an Android APK to a self-hosted F-Droid repo, regenerating the signed indexes so a desktop's package manager and a phone see the new version. Reach for it whenever you've built new artifacts and need them installable ('publish', 'release to the repo', 'update repos with latest artifacts', 'ship it to Basecamp/F-Droid', 'why does the device still see the old version'). Complements logos-basecamp-module / logos-mobile-app (which BUILD the artifacts) — this one DISTRIBUTES them."
---

Publishing is the step between "the artifact built" and "a user can install it." It is mechanical but has a handful of silent traps that make a publish *look* successful while shipping nothing. This skill is a reusable, config-driven publisher (`publish.sh`, alongside this file) plus the rules it encodes.

## What "publish" means here
- **Basecamp** (desktop): a **package repo** is a directory of `.lgx` files + an `index.json` (schemaVersion 2, one entry per module with url/size/sha256/rootHash/manifest) + a `logos-repo.json` catalog card (the https `indexUrl`). A host serves it; Basecamp adds the `logos-repo.json` URL as a package repository and installs/updates from it. **Regenerating the index = rescanning every `.lgx` in the dir**, so multiple apps coexist and updating one is "drop the new `.lgx`, rescan." The index lists **one entry per file**, so for a single-latest repo each module must be **one `.lgx` under a stable name that you overwrite** — a version-in-the-filename scheme accumulates and shows the module twice. `publish.sh` installs each as `logos-<module-name>-module.lgx` (overwrite) for exactly this reason.
- **F-Droid** (Android): a `repo/` of APKs + a signed `index-v1.json`, regenerated by `fdroid update`. Each app needs a `metadata/<applicationId>.yml`.

## The traps this skill encodes (each makes publish silently ship nothing)
1. **Publish the PORTABLE (`linux-amd64`) `.lgx`, never a `-dev` build.** A `-dev` module shows **"NOT AVAILABLE"** in Basecamp's package manager (it's for a local installer's different code path).
2. **F-Droid needs `metadata/<pkg>.yml` or the index comes out EMPTY** — `fdroid update` silently drops an APK with no metadata, and the device sees no app. (`publish.sh` auto-creates a minimal one.)
3. **Publish to the repo the client ACTUALLY reads.** It's easy to publish to a second repo nothing points at and see "success" while the device keeps the old version. Know which repo dir the served URL maps to.
4. **`https://` is mandatory** for a Basecamp repo URL (the downloader hard-rejects other schemes); use a self-signed cert for LAN.
5. **The APK is signed with your release key; the F-Droid *index* is re-signed with the repo keystore** — two different keys. An in-place update needs the same *app* signature as the installed version.
6. **Verify from the published path, not `dist/`.** Your build output being correct proves nothing about what the repo serves — refresh the client and confirm the new version appears.

## Use the bundled script
`publish.sh` (next to this file) does both halves and regenerates the indexes. It **publishes, it does not build** — build first with `logos-basecamp-module` (`nix build .#lgx-portable`, the *portable* variant) and `logos-mobile-app` (`assembleRelease` → signed APK).

```sh
# Basecamp only — one or more modules (a core + its view):
publish.sh --lgx dist/<app>_core-<ver>-linux-amd64.lgx --lgx dist/<app>-<ver>-linux-amd64.lgx

# F-Droid only — an APK + its applicationId (metadata auto-created if missing):
publish.sh --apk dist/<app>-<ver>-arm64.apk --apk-package co.example.<app> \
           --apk-name "<App>" --apk-summary "One-line description" --apk-category Internet

# Both at once (a full app release):
publish.sh --lgx .../core.lgx --lgx .../view.lgx \
           --apk .../app.apk --apk-package co.example.app --apk-name App --apk-summary "…"
```

**Config via env** (defaults suit the current host; override for another setup): `BASECAMP_REPO` (the served dir), `REPO_NAME`/`REPO_DISPLAY`, `REPO_HOST`/`REPO_PORT`, `FDROID_HOME`, `FDROID_BIN`. The script prints the resulting package lists; it also reminds you to confirm on a clean client.

## How an agent runs this
1. Build the artifacts (portable `.lgx`(s) + signed APK) per the build skills.
2. Invoke `publish.sh` with the artifact paths + (for the APK) the applicationId and a name/summary.
3. Read back the printed Basecamp package list + F-Droid version, and — per trap 6 — verify the served index over its URL (or a clean client) before reporting done.
4. If the served repo is fronted by a long-running server, it serves the files live; no restart is needed after republishing.

## Public storefront / catalog — two surfaces, and the card fields
A public catalog like `apps.vpavlin.xyz` (repo `vpavlin/logos-apps`, served via GitHub Pages) is **not one repo** — it has **two independent Basecamp surfaces, and you must update BOTH**. Updating one while the other is stale is trap #3 ("publish to the repo the client actually reads") in its public form: the module installs fine yet never appears on the site, or vice-versa.

- **What the Basecamp APP installs from:** a package repo under the site (`…/basecamp/logos-repo.json` → `indexUrl` = the raw `…/basecamp/index.json`). Drop the `.lgx` in, rescan → `index.json` (the same schemaVersion-2 generator as the LAN case; `url` base is the raw-githubusercontent `…/main/basecamp`). This makes the module *installable*.
- **What the WEBSITE's Basecamp tab lists from:** a **separate** catalog index published as a GitHub **release asset** on the catalog repo (`vpavlin/logos-basecamp-modules`, tag `index`, asset `index.json`), where each module's `.lgx` is itself a per-tag release asset (`<name>-v<ver>/<name>-<ver>.lgx`). The site generator (`gen_modules_page.py`) fetches THIS, not `basecamp/index.json`.
- **⚠️ Landmine — the "release-all" Action can wipe the catalog.** It DISCOVERS modules from `.gitmodules`; if that's empty (the fork-template default) and the index was seeded another way, running the Action rebuilds the index **empty, dropping every module**. Do NOT run it to add one module. Edit the `index` release asset **additively** by hand: create the per-module release tag(s) holding the `.lgx`, merge the new entries into the fetched index **preserving all others**, and re-upload (the asset MUST stay named `index.json` — uploading a differently-named file adds a stray asset, it doesn't replace the index).

### The storefront card (name / logo / Source pill)
A module renders a lowercase name, no logo, and no repo pill unless THREE things exist — same three whether on the site or in a package-manager card:
1. **Display name** = the `.lgx` manifest `display_name` (falls back to the lowercase module `name`). Set `"display_name"` in the module's `metadata.json`. Convention: the *view* (`ui_qml`) carries the nice name; the core shows plainly.
2. **Logo** = an `icon.png` **baked into the `.lgx`** (the site extracts it from the tar by basename → data-URI; missing → a lettered monogram). Set `"icon":"icon.png"` in `metadata.json` **and** place `icon.png` next to it. **GOTCHA: a nix flake only sees git-TRACKED files** — an untracked `icon.png` (or an uncommitted `metadata.json` edit) is invisible, and the module-builder silently builds the OLD committed manifest (it may even print `git add <path>`). `git add` the icon + metadata **before** `nix build .#lgx-portable`, then verify `tar tzf x.lgx | grep icon` and `tar xzOf x.lgx manifest.json`.
3. **Source pill** = an editorial `overrides.json` entry (in the site repo, keyed by module `name`) `{"repo":"https://…","featured":true}` — Basecamp manifests carry no repo URL, so this is override-only. Mirror the sibling entries.

## Where else this applies
Any self-hosted Logos distribution: a household LAN repo, a team's internal repo, or a public catalog repo whose index points at GitHub-release artifact URLs (swap `--lgx` local paths for released URLs and regenerate the index the same way). The Basecamp-index and F-Droid-metadata rules are identical regardless of app domain.

## Sources & evidence
- Basecamp index/catalog generation (scan every `.lgx` → `index.json` + `logos-repo.json`): the reference publish scripts `scripts/gen-lan-repo.sh` and the LAN host's `regen.sh` in [github.com/vpavlin/kym](https://github.com/vpavlin/kym) (memory `kym-lan-repo-publishing`, `logos-repo-publishing`).
- PORTABLE-not-`-dev` rule + "NOT AVAILABLE": `docs/logos-dev-notes.md` (memory `logos-repo-publishing`).
- F-Droid `metadata/<pkg>.yml` required or empty index; publish to the repo the device reads: reference `scripts/fdroid-publish.sh` (memory `kym-brand-logo`, the wrong-repo regression).
